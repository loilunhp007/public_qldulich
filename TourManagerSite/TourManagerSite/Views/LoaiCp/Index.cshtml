@model List<TourManager.Models.LoaiCp>

@{
    ViewData["Title"] = "Index";
}

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold">LOẠI CHI PHÍ</h6>
    </div>
    <div class="card-body">
        <div class="col-12 text-right">
            <a asp-controller="LoaiCp" asp-action="Create" class="btn btn-info">
                <i class="fas fa-plus"></i>
            </a>
        </div>

        <br />

        @if (Model.Count() > 0)
        {
            <table id="loaiCpTable" class="table table-bordered table table-striped" style="width:100%">
                <thead>
                    <tr>
                        <th>
                            Loại Chi Phí
                        </th>                        
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var loai in Model)
                    {
                        <tr>
                            <td>@loai.TenLoaiCp</td>
                            <td class="text-center">
                                <a asp-action="Edit" asp-route-id="@loai.MaLoaiCp" class="btn btn-success mx-2 my-2">
                                    <i class="far fa-edit"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@loai.MaLoaiCp" class="btn btn-danger mx-2 my-2">
                                    <i class="far fa-trash-alt"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>Rỗng</p>
        }
    </div>
</div>
